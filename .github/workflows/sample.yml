name: basic actions

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a String
        run: echo "Hello world"
      - name: multiline script
        run: |
          node -v
          npm -v
